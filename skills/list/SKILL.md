---
description: List all available event sources (built-in and user-registered).
allowed-tools: Bash
disable-model-invocation: true
---

Run this command and display the output to the user:

```
Bash(command="${CLAUDE_PLUGIN_ROOT}/scripts/event-listen.sh list")
```
